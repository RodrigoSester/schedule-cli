package schedule

import (
	"fmt"
)

func Hello(name string) string {
	// use := to declare and assign a variable
	// equals to ':=' = 'var message string'
	message := fmt.Sprintf("Hello, %v!", name)

	return message
}
